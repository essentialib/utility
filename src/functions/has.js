/**
 * implement
 * `iterable`에 `item`이 value 로 있는지 확인합니다. `iterable`이 hashtable 인 경우, value 중에서 존재성을 확인합니다.
 * @param item
 * @param iterable
 */
module.exports = function has(item, iterable) {
    // todo: 구현
}