/**
 * @example
 * mapPair([1, 2, 3, 4, 5], (cur, nxt) => cur + nxt);
 * // => [3, 5, 7, 9]
 * mapPair([1, 2, 3, 4, 5], (cur, nxt) => (cur + nxt) / 2);
 * // => [1.5, 2.5, 3.5, 4.5]
 * mapPair([1, 2, 3, 4, 5], (cur, nxt, i, j) => i + j);
 * // => [1, 3, 5, 7]
 *
 * @param iterable
 * @param iteratee
 * @return {}
 */
module.exports = function mapPair(iterable, iteratee) {
    let ret;

    // implement

    return ret;
}